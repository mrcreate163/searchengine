# 🚀 Полное руководство по развертыванию Search Engine на Timeweb

## Оглавление
1. [Введение](#введение)
2. [Что вам понадобится](#что-вам-понадобится)
3. [Подготовка домена](#подготовка-домена)
4. [Создание и настройка VPS на Timeweb](#создание-и-настройка-vps-на-timeweb)
5. [Установка необходимого ПО](#установка-необходимого-по)
6. [Развертывание приложения](#развертывание-приложения)
7. [Настройка SSL сертификата](#настройка-ssl-сертификата)
8. [Проверка работы](#проверка-работы)
9. [Управление приложением](#управление-приложением)
10. [Мониторинг и логи](#мониторинг-и-логи)
11. [Резервное копирование](#резервное-копирование)
12. [Решение проблем](#решение-проблем)

---

## Введение

Поздравляю! Вы готовы развернуть ваше первое приложение в облаке. Это руководство проведет вас через все шаги от регистрации на Timeweb до полностью работающего приложения с HTTPS.

**Что вы получите в результате:**
- ✅ Работающее приложение Search Engine в облаке
- ✅ Доступ по вашему домену (например, search.yourdomain.com)
- ✅ HTTPS с автоматическим обновлением сертификатов
- ✅ Автоматический перезапуск при сбоях
- ✅ Изолированные контейнеры для безопасности

**Примерное время:** 1-2 часа (для новичка)

---

## Что вам понадобится

### 1. Аккаунты и доступы
- ✅ Аккаунт на [Timeweb Cloud](https://timeweb.cloud/) (российский облачный провайдер)
- ✅ Домен (можно купить на Timeweb или у другого регистратора)
- ✅ SSH ключ (мы создадим его вместе)

### 2. На вашем компьютере
- Терминал (Linux/Mac) или PowerShell/Git Bash (Windows)
- Git установлен
- Текстовый редактор (VS Code, Sublime Text, или любой другой)

### 3. Стоимость
- **Timeweb Cloud VPS:** от 169₽/месяц (1GB RAM) до 499₽/месяц (2GB RAM, рекомендуется)
- **Домен на Timeweb:** от 99₽/год (.ru) до 499₽/год (.com)
- **SSL сертификат:** Бесплатно (Let's Encrypt)

---

## Подготовка домена

### Вариант 1: Домен на Timeweb

Если вы покупаете домен на Timeweb, DNS уже будет настроен автоматически. После создания VPS вам просто нужно будет:
1. Перейти в раздел **Домены** → **DNS-записи**
2. Добавить A-записи, указывающие на IP вашего VPS (см. далее)

### Вариант 2: Домен у другого регистратора

Если у вас домен на другом регистраторе (Namecheap, REG.RU, и т.д.):

1. Войдите в панель управления вашего регистратора
2. Найдите раздел управления DNS
3. Добавьте следующие записи (пока используйте временный IP 1.1.1.1, потом заменим):

```
Type    Host    Value           TTL
A       @       1.1.1.1         Automatic
A       www     1.1.1.1         Automatic
```

**Важно:** Мы вернемся сюда позже, когда получим IP адрес от Timeweb!

---

## Создание и настройка VPS на Timeweb

### Шаг 1: Создание SSH ключа (если еще не создан)

**На Linux/Mac:**
```bash
# Создать новый SSH ключ
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Нажмите Enter для сохранения в ~/.ssh/id_rsa
# Введите пароль (необязательно, но рекомендуется)

# Показать публичный ключ
cat ~/.ssh/id_rsa.pub
```

**На Windows (Git Bash или PowerShell):**
```bash
# Создать новый SSH ключ
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Показать публичный ключ
cat ~/.ssh/id_rsa.pub
```

**Скопируйте весь вывод команды (начинается с `ssh-rsa`)** - он понадобится на следующем шаге!

### Шаг 2: Регистрация на Timeweb Cloud

1. Перейдите на [Timeweb.Cloud](https://timeweb.cloud/)
2. Нажмите **Регистрация** в правом верхнем углу
3. Заполните форму регистрации
4. Подтвердите email
5. Пополните баланс на минимальную сумму (обычно от 100₽)

### Шаг 3: Создание облачного сервера

1. Войдите в панель управления Timeweb Cloud
2. В меню выберите **Облачные серверы** (или **Cloud Servers**)
3. Нажмите **Создать сервер**
4. Выберите конфигурацию:

**Тариф:**
- **S-2** (рекомендуется): 2GB RAM, 2 vCPU, 40GB SSD - 499₽/месяц
- Можно начать с **S-1** (1GB RAM, 1 vCPU, 25GB SSD - 169₽/месяц), но для поисковика может не хватить памяти

**Операционная система:**
- Выберите **Ubuntu 22.04 LTS** из списка образов

**Регион:**
- Выберите ближайший регион (Москва или Санкт-Петербург для минимальной задержки)

**Период оплаты:**
- Выберите удобный период (помесячно или с предоплатой)
- При оплате на 3+ месяца часто дают скидку

**Дополнительные опции:**
- **SSH ключ**: добавьте ваш публичный ключ (скопированный ранее)
- **Имя сервера**: можно указать `searchengine` или любое другое имя
- **Firewall**: можно настроить позже

5. Нажмите **Создать сервер**
6. Оплатите или подтвердите списание с баланса

**Подождите 1-5 минут** пока создается сервер (обычно быстрее чем у конкурентов).

### Шаг 4: Получение данных доступа

1. После создания сервера он появится в списке облачных серверов
2. Нажмите на название вашего сервера для просмотра деталей
3. **Скопируйте IP адрес** (например, 92.63.123.456)
4. Логин: `root` (по умолчанию для Ubuntu)
5. Пароль придет на email (если не использовали SSH ключ)

### Шаг 5: Настройка DNS

Вернитесь к настройкам домена и обновите DNS записи:

**Если домен на Timeweb:**
1. В панели управления перейдите в раздел **Домены**
2. Выберите ваш домен
3. Перейдите на вкладку **DNS-записи**
4. Добавьте/измените записи:

```
Type    Host    Value                    TTL
A       @       ВАШ_IP_АДРЕС_VPS        3600
A       www     ВАШ_IP_АДРЕС_VPS        3600
```

**Если домен у другого регистратора:**
Обновите A-записи в панели вашего регистратора, заменив временный IP на реальный IP VPS.

**Важно:** DNS изменения могут занять до 24 часов, но обычно работают через 15-30 минут.

### Шаг 6: Подключение к серверу

```bash
# Подключиться по SSH (замените YOUR_IP на ваш IP адрес)
ssh root@YOUR_IP
```

При первом подключении появится сообщение о добавлении хоста - введите `yes`.

Если вы настроили SSH ключ, вы сразу войдете на сервер. Если нет - введите пароль из email.

Если все настроено правильно, вы увидите приглашение командной строки Ubuntu! 🎉

---

## Установка необходимого ПО

Теперь вы на сервере. Выполните следующие команды по порядку:

### Шаг 1: Обновление системы
```bash
# Обновить список пакетов
apt update

# Обновить установленные пакеты
apt upgrade -y
```

### Шаг 2: Установка Docker
```bash
# Установить необходимые пакеты
apt install -y apt-transport-https ca-certificates curl software-properties-common

# Добавить официальный GPG ключ Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add -

# Добавить репозиторий Docker
add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"

# Обновить список пакетов
apt update

# Установить Docker
apt install -y docker-ce

# Проверить установку
docker --version
```

Вы должны увидеть версию Docker (например, `Docker version 24.0.7`).

### Шаг 3: Установка Docker Compose
```bash
# Скачать Docker Compose
curl -L "https://github.com/docker/compose/releases/download/v2.23.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

# Сделать исполняемым
chmod +x /usr/local/bin/docker-compose

# Проверить установку
docker-compose --version
```

Вы должны увидеть версию Docker Compose (например, `Docker Compose version v2.23.0`).

### Шаг 4: Установка Git
```bash
# Установить Git
apt install -y git

# Проверить установку
git --version
```

### Шаг 5: Настройка firewall

**Важно для Timeweb Cloud:** В панели управления Timeweb есть встроенный firewall. Рекомендуется использовать его в дополнение к UFW.

**Настройка Firewall в панели Timeweb (опционально, но рекомендуется):**
1. В панели управления сервера перейдите в раздел **Firewall**
2. Добавьте правила для портов: 22 (SSH), 80 (HTTP), 443 (HTTPS)

**Настройка UFW на сервере:**
```bash
# Включить UFW (Uncomplicated Firewall)
ufw enable

# Разрешить SSH
ufw allow 22/tcp

# Разрешить HTTP
ufw allow 80/tcp

# Разрешить HTTPS
ufw allow 443/tcp

# Проверить статус
ufw status
```

---

## Развертывание приложения

### Шаг 1: Клонирование репозитория
```bash
# Создать директорию для приложений
mkdir -p /opt/apps
cd /opt/apps

# Клонировать репозиторий
git clone https://github.com/mrcreate163/searchengine.git

# Перейти в директорию проекта
cd searchengine
```

### Шаг 2: Настройка конфигурации

```bash
# Создать .env файл из примера
cp .env.example .env

# Отредактировать конфигурацию
nano .env
```

**Важно! Измените следующие параметры:**

```bash
# Используйте СИЛЬНЫЕ пароли!
MYSQL_ROOT_PASSWORD=ваш_супер_сложный_пароль_12345
MYSQL_DATABASE=searchengine
MYSQL_USER=searchengine_user
MYSQL_PASSWORD=другой_супер_сложный_пароль_67890

SPRING_PROFILES_ACTIVE=prod

# Настройте сайты для индексации
INDEXING_SITES=[{"url":"https://www.example.com","name":"Example Site"}]

# ЗАМЕНИТЕ на ваш домен!
DOMAIN_NAME=yourdomain.com
SSL_EMAIL=your-email@example.com
```

**Советы по безопасности:**
- Используйте пароли длиной минимум 16 символов
- Комбинируйте буквы, цифры и специальные символы
- НЕ используйте личную информацию
- Храните пароли в безопасном месте (менеджер паролей)

**Для сохранения в nano:**
1. Нажмите `Ctrl + O` (сохранить)
2. Нажмите `Enter` (подтвердить имя файла)
3. Нажмите `Ctrl + X` (выход)

### Шаг 3: Запуск приложения

```bash
# Запустить деплой скрипт
./deploy.sh
```

Этот скрипт:
- ✅ Проверит конфигурацию
- ✅ Остановит старые контейнеры (если есть)
- ✅ Скачает базовые образы
- ✅ Соберет ваше приложение
- ✅ Запустит все сервисы
- ✅ Проверит работоспособность

**Ожидайте 3-5 минут.** Первый запуск занимает больше времени, так как скачиваются образы.

### Шаг 4: Проверка запуска

```bash
# Посмотреть статус контейнеров
docker-compose ps

# Все контейнеры должны быть в статусе "Up"
```

Вы должны увидеть:
```
NAME                   STATUS
searchengine_app       Up (healthy)
searchengine_mysql     Up (healthy)
searchengine_nginx     Up
searchengine_certbot   Up
```

### Шаг 5: Проверка доступности

```bash
# Проверить, отвечает ли приложение
curl http://localhost:8080/api/statistics
```

Если видите JSON ответ - отлично! Приложение работает.

---

## Настройка SSL сертификата

**Важно:** Убедитесь, что ваш домен уже указывает на IP сервера (DNS обновился). Проверить можно так:

```bash
# Проверить, что домен указывает на ваш IP
ping yourdomain.com
# Должен показать IP вашего сервера
```

Если ping не работает или показывает другой IP - подождите еще, пока DNS обновится.

### Когда DNS обновился:

```bash
# Вернуться в директорию проекта (если вышли)
cd /opt/apps/searchengine

# Запустить скрипт настройки SSL
./setup-ssl.sh
```

Этот скрипт:
- ✅ Настроит nginx для HTTP
- ✅ Получит бесплатный SSL сертификат от Let's Encrypt
- ✅ Настроит автоматическое перенаправление HTTP → HTTPS
- ✅ Настроит автоматическое обновление сертификатов

**Процесс занимает 2-3 минуты.**

---

## Проверка работы

### Проверка через браузер

Откройте в браузере:
1. **HTTP:** `http://yourdomain.com` - должен перенаправить на HTTPS
2. **HTTPS:** `https://yourdomain.com` - должен показать приложение
3. **WWW:** `https://www.yourdomain.com` - также должен работать

### Проверка SSL

В браузере:
1. Нажмите на замочек рядом с адресной строкой
2. Проверьте, что сертификат выдан Let's Encrypt
3. Проверьте срок действия (должен быть 90 дней)

### Проверка здоровья приложения

```bash
curl https://yourdomain.com/health
```

Должен вернуть: `{"status":"UP"}`

---

## Управление приложением

### Просмотр логов

```bash
# Логи всех сервисов
docker-compose logs -f

# Логи только приложения
docker-compose logs -f app

# Логи только MySQL
docker-compose logs -f mysql

# Логи nginx
docker-compose logs -f nginx

# Последние 100 строк
docker-compose logs --tail=100
```

### Перезапуск сервисов

```bash
# Перезапустить все
docker-compose restart

# Перезапустить только приложение
docker-compose restart app

# Перезапустить только MySQL
docker-compose restart mysql
```

### Остановка и запуск

```bash
# Остановить все
docker-compose down

# Запустить все
docker-compose up -d

# Остановить и удалить все данные (ОСТОРОЖНО!)
docker-compose down -v
```

### Обновление приложения

```bash
cd /opt/apps/searchengine

# Получить последние изменения
git pull

# Пересобрать и перезапустить
docker-compose build app
docker-compose up -d
```

### Просмотр использования ресурсов

```bash
# Использование CPU и памяти контейнерами
docker stats

# Использование диска
df -h

# Свободная память
free -h
```

---

## Мониторинг и логи

### Настройка ротации логов

Создайте файл для logrotate:

```bash
nano /etc/logrotate.d/searchengine
```

Добавьте:
```
/var/log/searchengine/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 644 root root
}
```

### Мониторинг производительности

```bash
# Установить htop для мониторинга
apt install -y htop

# Запустить htop
htop
```

### Проверка работы приложения (cron)

Создайте простой скрипт проверки:

```bash
nano /opt/apps/searchengine/healthcheck.sh
```

Добавьте:
```bash
#!/bin/bash
if ! curl -f https://yourdomain.com/health > /dev/null 2>&1; then
    echo "Application is down! Restarting..." | mail -s "SearchEngine Alert" your-email@example.com
    cd /opt/apps/searchengine
    docker-compose restart app
fi
```

Сделайте исполняемым и добавьте в cron:
```bash
chmod +x /opt/apps/searchengine/healthcheck.sh

# Добавить в cron (каждые 5 минут)
crontab -e

# Добавьте строку:
*/5 * * * * /opt/apps/searchengine/healthcheck.sh
```

---

## Резервное копирование

### Ручное создание бэкапа базы данных

```bash
# Создать директорию для бэкапов
mkdir -p /opt/backups

# Создать бэкап
docker-compose exec mysql mysqldump -u root -p$MYSQL_ROOT_PASSWORD searchengine > /opt/backups/backup-$(date +%Y%m%d-%H%M%S).sql

# Сжать бэкап
gzip /opt/backups/backup-*.sql
```

### Автоматическое создание бэкапов

Создайте скрипт:
```bash
nano /opt/apps/searchengine/backup.sh
```

Добавьте:
```bash
#!/bin/bash
BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d-%H%M%S)
cd /opt/apps/searchengine

# Создать бэкап
docker-compose exec -T mysql mysqldump -u root -p$MYSQL_ROOT_PASSWORD searchengine | gzip > $BACKUP_DIR/backup-$DATE.sql.gz

# Удалить бэкапы старше 7 дней
find $BACKUP_DIR -name "backup-*.sql.gz" -mtime +7 -delete

echo "Backup completed: backup-$DATE.sql.gz"
```

Сделайте исполняемым и добавьте в cron:
```bash
chmod +x /opt/apps/searchengine/backup.sh

# Ежедневный бэкап в 3:00
crontab -e
# Добавьте: 0 3 * * * /opt/apps/searchengine/backup.sh
```

### Восстановление из бэкапа

```bash
# Список бэкапов
ls -lh /opt/backups/

# Восстановить из бэкапа
gunzip < /opt/backups/backup-YYYYMMDD-HHMMSS.sql.gz | docker-compose exec -T mysql mysql -u root -p$MYSQL_ROOT_PASSWORD searchengine
```

---

## Решение проблем

### Приложение не запускается

```bash
# 1. Проверить логи
docker-compose logs app

# 2. Проверить конфигурацию
cat .env

# 3. Проверить, что MySQL работает
docker-compose ps mysql

# 4. Проверить подключение к MySQL
docker-compose exec mysql mysql -u root -p$MYSQL_ROOT_PASSWORD -e "SHOW DATABASES;"

# 5. Пересоздать контейнеры
docker-compose down
docker-compose up -d
```

### SSL сертификат не получается

**Проблема:** Certbot не может получить сертификат

**Решение:**
1. Проверьте, что DNS обновился: `ping yourdomain.com`
2. Проверьте, что порт 80 открыт: `ufw status`
3. Проверьте логи certbot: `docker-compose logs certbot`
4. Убедитесь, что nginx работает: `docker-compose ps nginx`
5. Попробуйте снова: `./setup-ssl.sh`

### Недостаточно памяти

**Симптомы:** Приложение падает, MySQL перезапускается

**Решение:**
1. Добавить swap:
```bash
# Создать swap файл 2GB
fallocate -l 2G /swapfile
chmod 600 /swapfile
mkswap /swapfile
swapon /swapfile

# Сделать постоянным
echo '/swapfile none swap sw 0 0' | tee -a /etc/fstab
```

2. Или увеличить тариф VPS:
   - В панели управления Timeweb Cloud → **Облачные серверы**
   - Выберите ваш сервер → **Изменить конфигурацию**
   - Выберите больший тариф
   - Подтвердите изменения (разница будет списана с баланса)

### База данных повреждена

```bash
# Остановить приложение
docker-compose stop app

# Попробовать восстановить таблицы
docker-compose exec mysql mysqlcheck -u root -p$MYSQL_ROOT_PASSWORD --auto-repair --all-databases

# Если не помогло - восстановить из бэкапа
gunzip < /opt/backups/backup-LATEST.sql.gz | docker-compose exec -T mysql mysql -u root -p$MYSQL_ROOT_PASSWORD searchengine

# Перезапустить
docker-compose up -d
```

### Низкая производительность

1. **Проверить использование ресурсов:**
```bash
docker stats
htop
```

2. **Оптимизировать MySQL:**
```bash
# Увеличить буферы в docker-compose.yml
# Добавить в сервис mysql:
environment:
  - MYSQL_INNODB_BUFFER_POOL_SIZE=512M
```

3. **Очистить старые данные:**
```bash
# Подключиться к MySQL
docker-compose exec mysql mysql -u root -p$MYSQL_ROOT_PASSWORD searchengine

# Удалить старые индексы (если нужно)
# Будьте осторожны с этими командами!
```

### Не могу подключиться по SSH

1. Проверьте IP адрес в панели управления Timeweb Cloud (Облачные серверы)
2. Проверьте firewall: `ufw status`
3. Попробуйте через веб-консоль Timeweb:
   - В панели управления → Облачные серверы → ваш сервер → **Консоль**
4. Если использовали пароль вместо SSH ключа, проверьте пароль из email

---

## Дополнительные рекомендации

### Безопасность

1. **Регулярно обновляйте систему:**
```bash
apt update && apt upgrade -y
```

2. **Создайте отдельного пользователя (не используйте root):**
```bash
adduser yourusername
usermod -aG sudo yourusername
usermod -aG docker yourusername
```

3. **Отключите вход по паролю (только SSH ключи):**
```bash
nano /etc/ssh/sshd_config
# Найдите и измените:
# PasswordAuthentication no
systemctl restart sshd
```

4. **Установите fail2ban:**
```bash
apt install -y fail2ban
systemctl enable fail2ban
```

### Производительность

1. **Настройте MySQL для производственного использования**
2. **Используйте CDN для статических файлов** (Cloudflare - бесплатно)
3. **Настройте мониторинг** (UptimeRobot - бесплатно)

### Масштабирование

Когда приложение вырастет:
1. **Увеличьте тариф VPS** - в панели управления Timeweb можно легко перейти на более мощный тариф
2. **Разделите базу данных и приложение** - используйте отдельный сервер для MySQL
3. **Добавьте балансировщик нагрузки** - для обработки большего трафика
4. **Используйте несколько серверов** - для высокой доступности

---

## Полезные команды - шпаргалка

```bash
# === Подключение к серверу ===
ssh root@YOUR_IP

# === Docker ===
docker-compose ps                 # Статус сервисов
docker-compose logs -f            # Логи (live)
docker-compose restart            # Перезапуск
docker-compose down               # Остановка
docker-compose up -d              # Запуск
docker stats                      # Использование ресурсов

# === Система ===
htop                              # Монитор процессов
df -h                             # Использование диска
free -h                           # Использование памяти
ufw status                        # Статус firewall

# === База данных ===
docker-compose exec mysql mysql -u root -p  # Подключиться к MySQL

# === Обновление приложения ===
cd /opt/apps/searchengine
git pull
docker-compose build app
docker-compose up -d

# === Бэкап ===
docker-compose exec mysql mysqldump -u root -p$MYSQL_ROOT_PASSWORD searchengine | gzip > backup.sql.gz
```

---

## Заключение

🎉 **Поздравляю!** Вы успешно развернули ваше первое production приложение!

**Что вы узнали:**
- ✅ Как настроить облачный сервер
- ✅ Как работать с Docker и Docker Compose
- ✅ Как получить и настроить SSL сертификаты
- ✅ Как настроить DNS и домен
- ✅ Как мониторить и обслуживать приложение
- ✅ Как создавать резервные копии

**Следующие шаги:**
1. Настройте мониторинг (UptimeRobot, Pingdom)
2. Настройте автоматические бэкапы
3. Добавьте больше сайтов для индексации
4. Оптимизируйте производительность по мере роста

**Полезные ресурсы:**
- [Timeweb Cloud: Документация](https://timeweb.cloud/help)
- [Timeweb Cloud: База знаний](https://timeweb.cloud/help/categories)
- [Docker Documentation](https://docs.docker.com/)
- [Let's Encrypt Documentation](https://letsencrypt.org/docs/)
- [Nginx Documentation](https://nginx.org/en/docs/)

**Нужна помощь?**
- Откройте Issue на GitHub
- Проверьте раздел "Решение проблем" в этом руководстве
- Напишите в поддержку Timeweb (если проблема с сервером)

Удачи с вашим проектом! 🚀
